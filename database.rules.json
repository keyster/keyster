{
  "rules": {
    "users": {
      "$uid": {
        ".read": "$uid === auth.uid",
        "settings": {
          ".write": "$uid === auth.uid",
          ".validate": "newData.hasChildren(['alphabet', 'length', 'N', 'p', 'r'])",
          "N": {
            ".validate": "newData.isNumber()"
          },
          "r": {
            ".validate": "newData.isNumber()"
          },
          "p": {
            ".validate": "newData.isNumber()"
          },
          "length": {
            ".validate": "newData.isNumber() && newData.val() <= 100"
          },
          "alphabet": {
            ".validate": "newData.isString() && newData.val().length <= 150"
          },
          "$other": {
            ".validate": "false"
          }
        },
        "services": {
          ".write": "$uid === auth.uid",
          "$id": {
            ".validate": "newData.hasChildren(['title', 'subtitle', 'description', 'salt', 'alphabet', 'length', 'N', 'p', 'r'])",
            "title": {
              ".validate": "newData.isString() && newData.val().length <= 500"
            },
            "subtitle": {
              ".validate": "newData.isString() && newData.val().length <= 100"
            },
            "description": {
              ".validate": "newData.isString() && newData.val().length <= 200"
            },
            "salt": {
              ".validate": "newData.isString() && newData.val().length <= 100 && !data.exists()"
            },
            "N": {
              ".validate": "newData.isNumber() && !data.exists()"
            },
            "r": {
              ".validate": "newData.isNumber() && !data.exists()"
            },
            "p": {
              ".validate": "newData.isNumber() && !data.exists()"
            },
            "length": {
              ".validate": "newData.isNumber() && newData.val() <= 100 && !data.exists()"
            },
            "alphabet": {
              ".validate": "newData.isString() && newData.val().length <= 256 && !data.exists()"
            },
            "$other": {
              ".validate": "false"
            }
          }
        },
        "archive": {
          "$id": {}
        },
        "$other": {
          ".validate": "false"
        }
      }
    }
  }
}
